# Nek5000

This directory contains the necessary scripts to run a Nek5000 simulation in `pipe_STAT`, along with a step-by-step log file (`Nek5000_log.pdf`) documenting the setup and execution process.

